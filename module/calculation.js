
var result = [];

switch(calculations(x, y)) {
    case 'plus':
        yourAnswer = x + y;
        break;
    case 'minus':
        yourAnswer = x - y;
        break;
    case 'multiply':
        yourAnswer = x * y;
      break;
    case 'divide':
      yourAnswer = x / y;
      break;
}
